@page
@model CASPARWeb.Areas.Admin.Pages.Campuses.UpsertModel
@{
}
<div class="card shadow border-0 mt-4 col-6 mx-auto">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-primary py-2">@(Model.objCampus.Id != 0 ? "Update " : "Add ") Campus</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row">
            @*<div asp-validation-summary="ModelOnly"></div>*@
            <input type="hidden" asp-for="objCampus.Id">
            <div class="border p-3">
                <label class="form-label">Campus Name</label>
                <div class="form-group mt-1">
                    <input id="inputCampusName" asp-for="objCampus.CampusName" class="form-control border-0 shadow" />
                </div>
                <div class="row pt-2 mt-3">
                    <div class="col-6">
                        <button type="submit" onclick="return ValidateInput()" class="btn btn-primary form-control">@(Model.objCampus.Id != 0 ? "Update " : "Add ")</button>
                    </div>
                    <div class="col-6">
                        <a asp-page="./Index" class=" btn btn-outline-primary border form-control">
                            Back to Campuses
                        </a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
<script>
    function ValidateInput() {
        if (document.getElementById('inputCampusName').value == "") {
            swal.fire('Error', 'Please Enter a Campus Name', 'error');
            return false;
        }
        return true;
    }
</script>